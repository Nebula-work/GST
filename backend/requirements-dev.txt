# Dev/test-only dependencies (NOT installed in production).
#   pip install -r requirements.txt -r requirements-dev.txt
# httpx backs fastapi.testclient.TestClient, used by tests/test_security.py for
# the HTTP-level tests. Without it those tests skip gracefully.
httpx>=0.27
